/* FeaturedProperties configuration */
export type SectionHeaderConfig = {
  title: string;
  subtitle: string;
};

export const SECTION_HEADER_CONFIG: SectionHeaderConfig = {
  title: "Featured Properties",
  subtitle: "Discover our top rated properties for your next vacation",
};
